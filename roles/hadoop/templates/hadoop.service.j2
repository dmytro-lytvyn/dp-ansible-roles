[Unit]
Description=Apache Hadoop
After=network.target

[Service]
Type=forking
User={{ hadoop_os_user_name }}
Group={{ hadoop_os_group_name }}
ExecStart={{ hadoop_link_dir }}/sbin/start-dfs.sh
ExecStop={{ hadoop_link_dir }}/sbin/stop-dfs.sh

[Install]
WantedBy=multi-user.target
