[Unit]
Description=Apache Hadoop - Namenode
After=network.target

[Service]
Type=forking
User={{ hadoop_os_user_name }}
Group={{ hadoop_os_group_name }}
ExecStart={{ hadoop_link_dir }}/bin/hdfs --daemon start namenode
ExecStop={{ hadoop_link_dir }}/bin/hdfs --daemon stop namenode
Restart=on-failure

[Install]
WantedBy=multi-user.target
